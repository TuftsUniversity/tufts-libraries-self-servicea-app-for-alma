{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Flask (launch via flask run)",
      "type": "debugpy",
      "python": "${workspaceFolder}/venv/Scripts/python.exe",
      "request": "launch",
      "module": "flask",
      "cwd": "${workspaceFolder}",
      "env": {
        "FLASK_APP": "run.py",          // or "app.py" or your entry file
        "FLASK_ENV": "development"
      },
      "args": [
        "run",
        "--no-debugger",
        "--no-reload",
        "--port", "5002",
        "--host", "127.0.0.1"
      ],
      "jinja": true,
      "justMyCode": true,
      "console": "integratedTerminal"
    }
  ]
}
